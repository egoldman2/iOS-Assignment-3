import SwiftUI

struct TradeView: View {
    var body: some View {
        VStack {
            Text("Trade Page")
                .font(.largeTitle)
                .padding()

            Spacer()
        }
        .navigationTitle("Trade")
    }
}

